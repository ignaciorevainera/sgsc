---
interface Props {
  class?: string;
  as?: "h2" | "h3" | "h4";
}

const { class: className = "", as: Tag = "h2" } = Astro.props;
---

<Tag class={`text-sm font-black uppercase italic ${className}`}>
  <slot />
</Tag>
