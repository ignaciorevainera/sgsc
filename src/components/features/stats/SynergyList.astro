---
import type { SynergyEntry } from "@/lib/stats/synergy";

interface Props {
  entries: SynergyEntry[];
}

const { entries } = Astro.props;
---

{
  entries.length === 0 ? (
    <p class="text-sm opacity-50">Sin datos suficientes</p>
  ) : (
    <ul class="divide-base-200 divide-y">
      {entries.map((e) => (
        <li class="grid grid-cols-[minmax(0,1fr)_auto_3rem] items-center gap-3 py-2">
          <span class="truncate text-sm font-black">{e.teammateNickname}</span>
          <span class="text-xs font-bold text-right opacity-60">
            {e.matchesTogether} partidos
          </span>
          <span class="text-success text-right text-sm font-black">{e.winRate}%</span>
        </li>
      ))}
    </ul>
  )
}
