# Credentials used by Playwright authenticated tests
TEST_EMAIL=test@example.com
TEST_PASSWORD=change-me

# Set to true only when you want e2e tests to persist real data (create player/match)
E2E_ALLOW_MUTATIONS=false
